# Octalの細道2 野望編

# 1. 振り返り……Forth処理系のまとめ

## 1.1 基本要素

> 1巻「黎明編」3.2「Forth処理系の概要」から、

* 辞書、エントリ、
* スタック
* インタプリタ、低レベルのインタプリタ、高レベルのインタプリタ
* アセンブラ
* 2次記憶

本書でのカバー範囲は、辞書・スタック・インタプリタまでで、アセンブラと2次記憶は実装対象ではない。

## 外部インタプリタ

> 3.3「Forth処理系を簡単に説明する」から

「数値かどうかのチェック」「辞書にあるかどうかのチェック」を前回と変更する。

INTERPRETの疑似コードを挙げる。

## 内部インタプリタ

> 3.6から

コード例を挙げて説明する。

## 旅程

* 端末からの入出力: 1黎明編と変わらず。
  * 数字出力はバグを取った。
* 内部インタプリタ: 1黎明編と変わらず。
* 行入力: 変更中(なので1黎明編と変わらず)
* 辞書検索: 本書で説明。
* 外部インタプリタ: 1.黎明編ではさわりだけ説明。本書で説明。

前巻「黎明編」ではここまでなんとか説明した。本書「野望編」では、

* 辞書エントリを作る
  ⁺ アセンブリ言語による記述と、
  + 辞書記述文法と変換ツール`makedict.sh`
* 疑似命令`lit`, `bra`, `bne`
* 外部インタプリタループ
  + accept: 行入力。黎明編で説明済
  + word: 行入力データから単語を切り出す。本書で説明
  + find: 辞書検索、本書で説明
  + number: 数値文字列を16bit integerに変換、本書で説明。
  + execute: ワードの実行、本書で説明
* 定義語, `:`(コロン)と`;`(セミコロン)
  + コンパイルと
  + コンパイルの動作
    + 辞書ヘッダ
    + リンク
    + do_list機械語命令
    + ワードリスト: ワードCFAへのアドレスの列
    + do_exit
  + 辞書エントリの変更
    + 優先度(precedence)フィールド
  + 外部インタプリタの変更
* 条件分岐: IF ... ELSE ... THEN
  + ワード定義による実装
  + ブランチ命令オフセットの計算
* 開発環境
  + コード処理のフロー図
  + 各ファイルの説明
  + ツールの説明
  + 実行環境・デバッグ方法

までを挙げる。

* 定義語: VARIABLE, CONSTANT
* DO ... WHILE(ループ)
* 算術演算

は次巻「雌伏編」以後まで持ち越しです。済みません。

# 2. 辞書エントリを作る

